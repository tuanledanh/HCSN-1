export enum PopupDetailMode {
    CREATE = 'CREATE',
    UPDATE = 'UPDATE'
}

export enum Language {
    VN = 'VN',
    EN = 'EN'
}

export enum TableColumnType {
    SELECTION = 'selection',
    INDEX = 'index',
    EXPAND = 'expand',
    NORMAL = 'normal'
}

export enum UpdateRowAction {
    ADD = 'ADD',
    DELETE = 'DELETE'
}

export enum FixedAssetPopupMode {
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
    REPLICATE = 'REPLICATE'
}

export enum ActionMode {
    UPDATE = 0,
    CREATE = 1,
    DELETE = 2,
    NOCHANGE = 3
}

export enum InputType {
    TEXT = 'text',
    NUMBER = 'number',
    PERCENT = 'percent',
    CODE = 'code'
}

export enum TooltipType {
    TOP = 'top',
    BOTTOM = 'bottom',
    LEFT = 'left',
    RIGHT = 'right'
}

export enum KeyName {
    ENTER = 'Enter',
    ESCAPE = 'Escape',
    ARROW_UP = 'ArrowUp',
    ARROW_DOWN = 'ArrowDown'
}

export enum ToastMessageType {
    SUCCESS = 'success',
    ERROR = 'error',
    WARNING = 'warning',
    INFO = 'info'
}
